# PRD.md —— 学分规划助手 · 产品需求文档（一期）

> 项目：大学生学分计算与规划网站
> 版本：一期（MVP）　｜　撰写日期：2026-09-19（Day 4）　｜　依据：research.md
> 一句话：输入已修课程，**按板块**算清"修够没有、还差多少"，据此判断哪些课必须选、哪些不必再选。
> 研究结论（见 research.md 第三节）：本产品 = 免登录的轻体验 + 分板块缺口视角 + 贴合国内计分习惯。

---

## 一、背景与要解决的问题

**问题有两个**：

1. **算不清学分**：大学学分按"板块"划分（如必修、专业选修、通识选修等），学生通常只知道"总学分还差多少"，不知道**哪个板块还差学分、哪类课还用不用再选**，于是每学期选课都得自己拿培养方案手算一遍。
2. **看不见培养方案里的特殊条件**：板块要求往往藏在一大段文字中间（例如"通识选修须含 2 学分艺术类"），学生不容易注意到，选课时容易漏。**一期的回应**：给每个板块加一个"特殊条件备注"框，用户把特殊要求抄一次、贴在板块卡片上，每次查看缺口时都会看到（见 F1）。二期由文件解析自动提取（见第五节后续待办）。

**证据**（详见 research.md 第三节）：
- 广东外语外贸大学教务处官方文档明确写道：正方系统对通选课学分**统计不全**，要求学生在系统中**"自行核算学分"**——学生被迫自己算账，这是官方级证据。
- 教务系统只有"已获学分"查询，**没有**"还差多少、下学期怎么选"的规划视角。
- 课表类 App（超级课程表/WakeUp）以周课表为主界面，**没有**学分完成度视图。

**产品定位（一期只做这一件事）**：把"分板块的学分账"算清楚，直接回答"哪些板块还差、还差多少、这个板块的课还要不要选"。一期**不承诺**自动"读懂"培养方案原文——特殊条件由用户抄进"板块备注"人工兜底（见 F1）。

## 二、目标用户与使用场景

**目标用户**：苦恼于计算学分、规划上课的同学——以在校大学生为主，尤其是学分板块要求复杂、培养方案文字繁琐的专业的学生。

**使用场景**：
1. **每学期选课前**：查看各板块缺口 → 判断下学期该选哪类课、哪类课不必再选。
2. **每学期成绩出完后**：更新已修课程 → 各板块缺口自动重算。

**一期明确不适用的场景**：自动读懂培养方案原文（一期用"板块备注"人工兜底）；自动对接学校教务系统。

## 三、一期功能清单

| 编号 | 功能 | 用户动作 | 可观察结果 | 预计工作量 |
|---|---|---|---|---|
| F1 | 板块设置 | 新增 / 改名 / 删除板块；给每个板块填"要求学分"；填"特殊条件备注"（如"须含 2 学分艺术类"） | 能新建至少 3 个板块；要求学分与备注都能保存并显示；备注在板块卡片上单独一行展示、与学分数值同屏可见；删除板块时提示该板块下已有课程如何处理 | 1 天 |
| F2 | 课程录入（含状态） | 录入课程：名称 / 学分 / 所属板块 / 状态（**已修 / 计划修**）/ 成绩（已修时可选填）；可编辑、可删除 | 新增课程立即出现在列表；编辑保存后生效；删除后列表与统计同步更新；已修与计划修在列表上可区分；**成绩低于 60 分的课程不计入已修学分**，仍在列表中标注"不及格·不计学分" | 1 天 |
| F3 | **板块缺口与选课结论（核心）** | 查看每个板块：已修学分 / 要求学分 / 还差多少；并看到结论"**这个板块的课还要不要选**" | 缺口 > 0 的板块标红显示"还差 N 学分"，并提示"需从计划修课程中自行挑选，修满 N 学分"；修够的板块显示"已修够"，其下的计划修课程统一标记"**可不选**"；录入 3 门分属不同板块的课程后，各板块数字与手算一致 | 1 天 |
| F4 | 总分与 GPA（辅助信息） | 查看当前已修总学分、加权平均分、GPA；切换 GPA 换算规则 | 三项数值按录入数据计算，抽查 2 门课时结果与手算一致；切换换算规则后 GPA 数值随之变化 | 半天 |
| F5 | 本地保存 | 录入数据后刷新或关闭页面再打开 | 数据仍在，不需要注册登录；页面固定显示"数据仅保存在本机浏览器"的提示 | 半天 |
| F6 | 手机可用 | 用手机打开网站，完成录入与查看 | 375px 宽度下无横向滚动，按钮可点、输入框可输入 | 半天 |

**合计约 5 天**（含"课程状态 + 选课结论"增加的约半天），对应 Day 7 交付 MVP 的排期（Day 5–6 技术选型与搭骨架，Day 7 完成主线）。

### 数据规则（一期固定，不再讨论）

1. **学分**：按 0.5 的整数倍录入，允许一位小数（如 0.5 / 1.5 / 3）——很多课是 0.5 学分。
2. **成绩**：0–100 的整数。"已修"且低于 60 分的课程视为不及格，**不计入已修学分**，仍在列表中标"不及格·不计学分"。
3. **成绩为空的已修课程**（免修、"通过/不通过"等）：计入已修学分，但**不计入加权平均与 GPA**。
4. **板块"要求学分"未填**：该板块显示"未设置"，不参与缺口计算（避免被当成 0 而误判"已修够"）。
5. **缺口 ≤ 0**：显示"已修够（超出 X 学分）"，**不出现负数缺口**。
6. **已修总学分**：所有计入的已修课程学分之和（含超出板块要求的部分）。

## 四、验收标准（Day 7 交付时逐条检查）

- [ ] 无需注册登录即可使用，打开网页就能操作
- [ ] 能新建至少 3 个板块，并给每个板块填写要求学分
- [ ] 每个板块能填写"特殊条件备注"，并在板块卡片上直接看到
- [ ] 能录入课程（名称 / 学分 / 所属板块 / 状态 / 成绩），可编辑、可删除
- [ ] 课程可标记"已修 / 计划修"，列表上二者可以区分
- [ ] 每个板块正确显示：已修学分 / 要求学分 / 还差多少
- [ ] 缺口 > 0 的板块有明确标识（标红 +"还差 N 学分"），并提示"需从计划修课程中自行挑选，修满 N 学分"
- [ ] 修够的板块显示"已修够"，其下的计划修课程被标记为"可不选"
- [ ] 录入 3 门分属不同板块的课程后，各板块统计结果与手算结果一致
- [ ] 显示当前已修总学分、加权平均分、GPA
- [ ] GPA 的换算规则可切换，切换后数值同步变化
- [ ] 刷新页面后数据仍在（本地保存生效）
- [ ] 手机宽度（375px）下可正常录入与查看
- [ ] 能录入带小数的学分（如 1.5 学分），统计结果正确
- [ ] 成绩低于 60 分的课程不计入已修学分，并在列表中标"不及格·不计学分"
- [ ] 成绩为空的已修课程只计学分，不计入加权平均与 GPA
- [ ] 板块未填"要求学分"时显示"未设置"，不显示为 0
- [ ] 已修超出要求时显示"已修够（超出 X 学分）"，不出现负数缺口
- [ ] 录入 0 学分课程或未填成绩的课程时，页面不报错、统计结果不乱
- [ ] 页面明确提示"数据只保存在本机浏览器，不上传服务器"

## 五、本期不做 + 后续待办

### 本期不做（每条都写明理由）

| 不做的功能 | 理由 |
|---|---|
| ❌ 培养方案文件上传 / 自动解析 | 需解析 PDF/Word/图片，各校格式不一，准确率不可控；估 3–5 天，会挤掉核心的缺口计算。挪入"后续待办"第 1 条 |
| ❌ 对接教务系统、自动导入成绩 | 需要学号密码，有合规与安全风险，技术量巨大 |
| ❌ 注册 / 登录 / 云端同步 | 一期用浏览器本地存储即可满足单人使用 |
| ❌ 智能选课推荐（"哪门课更值得选 / 哪个老师更好"） | 需要内置各校课程库与评价数据；一期只按"计划修课程 + 学分缺口"给出"还需选几门、哪些可不选"的提示，不做课程优劣推荐 |
| ❌ 内置各校培养方案库 | 每校每专业都不同，数据维护是无底洞 |
| ❌ 多学期对比图表、导出成绩单、分享海报、小程序/App 版本 | 超出"算清板块缺口"的一期定位 |

### 后续待办（二期候选，按优先级排序）

1. **培养方案文件解析**（最高优先级）。依据：本项目服务的正是"培养方案写得很复杂、特殊要求藏在一大段文字里"的学生；如果全靠学生自己手填，等于他本来就看懂了培养方案，网站的价值就少了一半。二期把"读懂"这一环补上：上传培养方案 → 系统给出板块、要求学分与"特殊条件"建议 → 用户确认修正，自动填进一期已有的板块备注框。
2. 多学期 / 毕业时间线视图（看清每个学期怎么排课）。
3. 导出与分享（导出成绩单、生成海报）。
4. 云端账号与多设备同步。
5. **选课门数建议**：由系统按学分自动算出"最少还需要选几门"（把缺口按计划修课程的学分从大到小累加，得出最少门数），帮学生规划选课。工期足够时优先提前做，一期只提示"需修满 N 学分，自行挑选"。

## 六、风险与不确定点

| 风险 / 不确定点 | 一期如何应对 |
|---|---|
| GPA 换算规则各校不同（4.0 / 5.0 / 等级制） | 做成可切换规则；页面标注"换算规则各校不同，以本校教务处规定为准"，不宣称标准答案 |
| 毕业总学分与各板块要求学分各校不同（常见总学分 140–170） | 全部由用户自己填写，不内置任何学校的默认值 |
| 学生是否愿意手动录入（未做用户访谈） | 一期先自己使用，再找 2–3 位同学试用并收集反馈；这也是二期文件解析的立项依据 |
| 板块命名差异大（如"通识教育选修课模块"） | 板块名完全自定义，不限命名方式 |
| 培养方案的特殊条件靠用户自己抄录，可能漏抄或抄错 | "特殊条件备注"显示在板块卡片上，每次查看缺口都会看到，降低漏看概率；二期文件解析上线后自动提取填充 |
| 数据只存在本机浏览器，清理浏览器数据会丢失 | 页面上明确提示"数据仅保存在本机"；导出/导入作为后续待办第 3 条 |

## 七、成功指标（一期）

1. 作者本人能在一次选课前 **10 分钟内**完成板块与课程录入，并得出"哪个板块还要修"的结论。
2. 请 3 位同学试用后，**至少 2 位**能说出"我看懂自己还差什么了"。
3. 无"算错了"的反馈：抽查 3 次，各板块统计结果与手算一致。

---

> 本文档是一期的施工范围与验收依据。超出本文档的功能一律不做，需要新增时先更新本文档再动手。
